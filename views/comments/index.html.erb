<html>
<head>

	<script src="http://ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
	<script src="http://www.seanmccambridge.com/tubular/js/jquery.tubular.1.0.js"></script>
	<script> 

document.createElement('video');

$(document).ready(function(){
  $('a[href="#comment_title"]').click(function() {
    if (location.pathname.replace(/^\//,'') == this.pathname.replace(/^\//,'')
    && location.hostname == this.hostname) {
      var $target = $(this.hash);
      $target = $target.length && $target
      || $('[name=' + this.hash.slice(1) +']');
      if ($target.length) {
        var targetOffset = $target.offset().top;
        $('html,body')
        .animate({scrollTop: targetOffset}, 1000);
       return false;
      }
    }
  });
});

	$(document).ready(function(){
  $('a[href="#commentname"]').click(function() {
    if (location.pathname.replace(/^\//,'') == this.pathname.replace(/^\//,'')
    && location.hostname == this.hostname) {
      var $target = $(this.hash);
      $target = $target.length && $target
      || $('[name=' + this.hash.slice(1) +']');
      if ($target.length) {
        var targetOffset = $target.offset().top;
        $('html,body')
        .animate({scrollTop: targetOffset}, 1000);
       return false;
      }
    }
  });
});

		$(document).ready(function(){
  $('a[href="#commentbox"]').click(function() {
    if (location.pathname.replace(/^\//,'') == this.pathname.replace(/^\//,'')
    && location.hostname == this.hostname) {
      var $target = $(this.hash);
      $target = $target.length && $target
      || $('[name=' + this.hash.slice(1) +']');
      if ($target.length) {
        var targetOffset = $target.offset().top;
        $('html,body')
        .animate({scrollTop: targetOffset}, 1000);
       return false;
      }
    }
  });
});

				$(document).ready(function(){
  $('a[href="#titleb"]').click(function() {
    if (location.pathname.replace(/^\//,'') == this.pathname.replace(/^\//,'')
    && location.hostname == this.hostname) {
      var $target = $(this.hash);
      $target = $target.length && $target
      || $('[name=' + this.hash.slice(1) +']');
      if ($target.length) {
        var targetOffset = $target.offset().top;
        $('html,body')
        .animate({scrollTop: targetOffset}, 1000);
       return false;
      }
    }
  });
});

		$(document).ready(function(){
  $('a[href="#containerc"]').click(function() {
    if (location.pathname.replace(/^\//,'') == this.pathname.replace(/^\//,'')
    && location.hostname == this.hostname) {
      var $target = $(this.hash);
      $target = $target.length && $target
      || $('[name=' + this.hash.slice(1) +']');
      if ($target.length) {
        var targetOffset = $target.offset().top;
        $('html,body')
        .animate({scrollTop: targetOffset}, 1000);
       return false;
      }
    }
  });
});

	$(document).ready(function(){
  $('a[href="#containerd"]').click(function() {
    if (location.pathname.replace(/^\//,'') == this.pathname.replace(/^\//,'')
    && location.hostname == this.hostname) {
      var $target = $(this.hash);
      $target = $target.length && $target
      || $('[name=' + this.hash.slice(1) +']');
      if ($target.length) {
        var targetOffset = $target.offset().top;
        $('html,body')
        .animate({scrollTop: targetOffset}, 1000);
       return false;
      }
    }
  });
});



</script>
</head>

<body>

<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <!-- <div class="container-fluid">
    <!-Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="#">in memoriam.</a>
    </div> 

    <!-- Collect the nav links, forms, and other content for toggling -->
      <ul class="nav navbar-nav">
      	<!-- <li><%= link_to "V I E W", comments_path %></li> -->
      	<li><a href="#containerd">V I E W </a></li>
       <!--  <li><%= link_to "N E W", comments_path %></li> -->
        <li><a href="#containerc">N E W </a></li>
      </ul>
  
</nav>




<div id="wrapper">
	<div id="wrapper1">

		<%= video_tag "abandoned.mp4", id:"abandoned", autoplay: true, loop: true, muted: true, size: "1200x700" %>
	

<div class="rowheader"> <!-- transform:rotate(0deg);" data-500="background-color:rgb(255,0,0);transform:rotate(360deg);"> -->
	<div id="titleb"><h1 class="header"> D E A R   <span class="title">J O J O </span></h1></div>

<a href="#commentbox"><div id="arrow1"></div></a>

</div>
</div>

<div id="wrapper2">
	<div id="commentbox">

		
		<div id="containerc">
		<%= form_for @comment do |f| %>

		<div id="namewrapper">
			<div id="commentname">
				 N A M E <br>
				<%= f.text_field :name,  :cols => 50  %> <br />
			

          <div id="imageattach">
        I M A G E<%= f.file_field :image %>
        </div>  


		
			<br> 
				M E S S A G E  <br><%= f.text_area :message, :cols => 50, :rows => 8 %> <br />
      </div>

				<%= f.submit "S U B M I T" %>
				<a href="#containerd"><div id="arrow"></div></a>
			</div> <br /> <br />
    </div>
			
		</div>
	
		<% end %>

	</div>
</div>
</div>


</div>

<hr>

<hr>

<div id="line"></div>

<div id="containerd">
	<br />

<div id="comment_title">
C O M M E N T S </div> <br> 

<div class="container" > 
	<% @comments.reverse.each do |c| %>
		<div class="row">

			<div class="commentbubbles">
				<!-- <div class="commentbubbles col-md-4 col-md-offset-4"> -->
				<%= c.message %><br>
				<%= image_tag c.image.url(:small) %><br>
				
				<br><span text-align="center"><hr>-<%= c.name %></span><br>
				<%= c.created_at %>
			</div>

		</div> <br /> <br /> <br /> <br /> <br /> 
	</div>
	<% end %>
</div>
</div>
</div>


</body>
